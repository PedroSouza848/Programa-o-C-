#include <stdio.h>

int main () {
    int nota1 = 8;  //Número inteiro (Dãã)
    int nota2 = 6;
    float media = 7.0; //Números reais (Ex: 1.45, 8.98, 5.41)
    double peso = 59.6; //Mesma coisa que o float, mas para números maiores
    char texto = 'Opa';  // char = caracteres (O mesmo que str do Python)
    char nome[20] = 'Pedro'; //Esse [20] significa o número de caracteres que a variável vai ter
    
}